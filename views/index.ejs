<!DOCTYPE html>
<html lang="en">
<head>
  
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" 
  href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" 
  integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

  <link rel="stylesheet" 
  href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" 
  integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" 
  crossorigin="anonymous"
  >
  <link rel="stylesheet" href="css/style.css">
  <title>  Short URL </title>
</head>
<body>
<nav class="navbar navbar-light bg-dark">
    <a class="navbar-brand text-white" href="#">
        <img src="/images/link-icon.png"  class="d-inline-block align-top" alt="" loading="lazy">
         Short URL
    </a>
</nav>
  <div class="container">
    <form action="/create-short-url" method="POST" class="my-4 form-inline">
      <!--<div class="d-flex justify-content-center"></div>-->
      <input required placeholder="Enter a long URL" type="url" name="fullUrl" id="fullUrl" class="form-control col mr-2">
      <!--<div class="d-flex justify-content-center py-2"> </div>-->
      <button class="btn btn-success" type="submit"> <i class="fas fa-link"></i> Get Short URL  </button>

      <% if (shortUrl.length > 0) {%>
      <table class="table table-striped table-responsive text-white py-5">
        <thead>
         <tr>
            <th>Full URL</th>
            <th>Short URL</th>
            <th>Clicks</th>
            <th> Delete </th>
          </tr>
        </thead>
        <tbody>
          <% shortUrl.forEach(shortUrl => { %>
            <tr>
              <td><a class="text-white" href="<%= shortUrl.fullUrl %>"><%= shortUrl.fullUrl %></a></td>
              <td><a class="text-white" href="/shorturl/<%= shortUrl.shortUrl %>"><%= shortUrl.shortUrl %></a></td>
              <td><%= shortUrl.clicks %></td>
              <td><a class="btn btn-danger" href="/url/delete/<%=shortUrl._id%>"> <i class="fas fa-trash"></i> </a></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      <% } %>
    </form>
  </div>
</body>
</html>